# agent-runner-CLI
The CLI should connect to RustGrid, claim an agent ticket, generate a working prompt for Codex, run the agent locally in a Git repository, create a branch, commit changes, push to GitHub, open a pull request, and report all steps back to RustGrid.
