{
  "name": "rustgrid-agent-quality-gates",
  "version": "1.3.0",
  "private": true,
  "description": "npm entry points for the rustgrid-agent repository quality gates",
  "scripts": {
    "test": "sh scripts/cargo-quality-gate.sh test",
    "build": "sh scripts/cargo-quality-gate.sh build"
  }
}
